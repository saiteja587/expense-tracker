# Populated automatically when you connect a Neon database to this
# project from Vercel's Storage tab (Marketplace > Neon). You shouldn't
# need to set this by hand on Vercel itself.
# For local dev, copy this file to .env.local and paste in the
# connection string shown in Vercel: Settings > Environment Variables
# (or the Neon console directly).
DATABASE_URL=
